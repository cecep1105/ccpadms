Django>=5.0,<5.1
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-environ>=0.11
ldap3>=2.9
PyMySQL>=1.1
pyzk>=0.9

# WebSocket real-time (console iclock) -- lihat iclock/consumers.py & README
channels>=4.1
channels_redis>=4.2
daphne>=4.1  # ASGI server utk produksi (runserver dev otomatis pakai ini juga kalau terinstall)

# mclock -- Mobile Attendance (monitoring MSSQL eksternal), lihat README
pymssql>=2.3
cryptography>=42.0  # enkripsi password MSSQL (mclock/crypto_utils.py)

# Alternatif driver MySQL performa lebih baik (opsional, perlu libmysqlclient-dev
# di OS). Kalau dipakai: install ini, lalu hapus 2 baris pymysql di config/__init__.py
# mysqlclient>=2.2

# mattendance -- Face Verification (mattendance/face_utils.py). PENTING:
# 'dlib' TIDAK punya wheel prebuilt (dikonfirmasi -- cuma ada source dist
# di PyPI), instalasi WAJIB compile dari sumber & BUTUH:
#   1. Visual C++ Build Tools (komponen "Desktop development with C++")
#   2. CMake terinstall & ada di PATH
#   3. Waktu tunggu lama (bisa 10-30+ menit) -- lihat README bagian
#      "Face Verification" untuk detail & alternatif kalau instalasi gagal.
face_recognition>=1.3
dlib>=19.24
# PENTING: numpy WAJIB < 2.0 -- wheel dlib prebuilt (mis. dlib-19.24.1-cp310-cp310-win_amd64.whl)
# umumnya dikompilasi terhadap ABI numpy 1.x. Kalau numpy ter-upgrade ke 2.x,
# dlib menolak array gambar dengan error "RuntimeError: Unsupported image
# type, must be 8bit gray or RGB image" WALAU shape/dtype array-nya sudah
# benar (dikonfirmasi lewat laporan komunitas persis kasus yang sama:
# dlib 19.24.99 + numpy 2.0.0 + Windows 11, error identik). Kalau sudah
# terlanjur ter-install numpy 2.x, jalankan:
#   pip install "numpy<2"
numpy>=1.26,<2.0
Pillow>=10.0

# Celery -- lempar proses CPU-intensive (face recognition) ke worker
# terpisah, lihat config/celery.py & mattendance/tasks.py. Reuse Redis yang
# sama dengan Channels (config/settings.py sudah diatur pakai DB index beda).
celery>=5.3
redis>=5.0  # client Python utk Celery bicara ke Redis broker

# iclock PUSH SDK -- file locking lintas platform (Windows & Linux) utk
# tulis text file harian (masterattlog/dst) yang bisa ditulis BERSAMAAN
# oleh banyak device sekaligus. Lihat iclock/pushsdk_writer.py & test/myrule.md.
portalocker>=2.8